#!/usr/bin/python3

from csl import Console, os, sys
from trm import execute

import obj

def main():
    obj.wd = "/var/lib/botd"
    c = Console()
    c.scan("irc")
    return c.cmd(" ".join(sys.argv[1:]))

execute(main)
os._exit(0)
